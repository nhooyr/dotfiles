#!/bin/sh
set -eu

if [ "$#" -eq 0 ]; then
  set -- -
fi

nvr --servername "/tmp/nvim-$NVIM_SESSION" --remote-wait "$@"
# Once neovim supports:
# nvim --server "/tmp/nvim-$NVIM_SESSION" --remote-wait "$@"
